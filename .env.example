# Copy to .env and fill in. .env is gitignored - never commit it.

# Which freezer this instance reads: 0 = CryoPlus 2, 1 = CBS3000
FREEZER=0
SERIAL_PORT=/dev/ttyUSB0
LOGFILE=log.log

# MQTT broker (Home Assistant)
MQTT_BROKER=192.0.2.1
MQTT_PORT=1883
MQTT_USERNAME=user
MQTT_PASSWORD=pass
